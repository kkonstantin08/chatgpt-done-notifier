{
  "name": "chatgpt-done-notifier",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that notifies when ChatGPT finishes responding.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node scripts/build.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "typescript": "^5.8.3"
  }
}
